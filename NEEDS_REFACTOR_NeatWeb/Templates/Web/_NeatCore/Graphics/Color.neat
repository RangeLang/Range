 #Color {
    function hsl(hue: Int, saturation: Int, lightness: Int, alpha: Int?)
    function oklch(lightness: Int, chroma: Int, hue: Int, alpha: Int?)
    function rgb(red: Int, green: Int, blue: Int, alpha: Int?)
}
