#Head {}


#Meta: Head {
  @title(text _: String)
  @charset(text _: String = "utf-8")
  @viewport(text _: String = "width=device-width, initial-scale=1.0")
}

#Stylesheet: Head {
  init(text _: String)
}
