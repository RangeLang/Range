construct JSONKeyedDecodingContainer: KeyedDecodingContainer {
    let fields: [JSONField]

    function decode(_ type: Bool.Type, forKey key: String) -> Result<Bool, DecodingError> {
        switch value(forKey: key) {
        case .success(let value):
            let container = JSONSingleValueDecodingContainer(value: value)
            return container.decode(Bool.self)
        case .failure(let error):
            return .failure(cause: error)
        }
    }

    function decode(_ type: Float.Type, forKey key: String) -> Result<Float, DecodingError> {
        switch value(forKey: key) {
        case .success(let value):
            let container = JSONSingleValueDecodingContainer(value: value)
            return container.decode(Float.self)
        case .failure(let error):
            return .failure(cause: error)
        }
    }

    function decode(_ type: Int.Type, forKey key: String) -> Result<Int, DecodingError> {
        switch value(forKey: key) {
        case .success(let value):
            let container = JSONSingleValueDecodingContainer(value: value)
            return container.decode(Int.self)
        case .failure(let error):
            return .failure(cause: error)
        }
    }

    function decode(_ type: String.Type, forKey key: String) -> Result<String, DecodingError> {
        switch value(forKey: key) {
        case .success(let value):
            let container = JSONSingleValueDecodingContainer(value: value)
            return container.decode(String.self)
        case .failure(let error):
            return .failure(cause: error)
        }
    }

    function decode(_ type: UUID.Type, forKey key: String) -> Result<UUID, DecodingError> {
        switch value(forKey: key) {
        case .success(let value):
            let container = JSONSingleValueDecodingContainer(value: value)
            return container.decode(UUID.self)
        case .failure(let error):
            return .failure(cause: error)
        }
    }

    function decode<T: Decodable>(_ type: T.Type, forKey key: String) -> Result<T, DecodingError> {
        switch value(forKey: key) {
        case .success(let value):
            let decoder = JSONDecoder(value: value)
            return T(from: decoder)
        case .failure(let error):
            return .failure(cause: error)
        }
    }

    function value(forKey key: String) -> Result<JSONValue, DecodingError> {
        for field in fields {
            if field.key == key {
                return .success(result: field.value)
            }
        }

        return .failure(cause: .failed)
    }
}
