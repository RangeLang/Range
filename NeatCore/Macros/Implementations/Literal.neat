macro literal<T>(): Init { target, diagnostics in
    if target.application.arguments.count == 1 && target.application.arguments[0].type == T.self {
        target.application.replace(with:
            target.application.arguments[0].expression
        )
    }
}
