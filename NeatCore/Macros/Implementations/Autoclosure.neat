macro autoclosure: Attached<Parameter> { target, diagnostics in
    target.type.rewrite(Closure(target.type))

    target.argument.rewrite(
        Closure(
            body: {
                target.argument.expression
            }
        )
    )
}
