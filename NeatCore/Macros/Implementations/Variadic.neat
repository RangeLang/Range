macro variadic(): Parameter { target, diagnostics in
    target.parameter.type.rewrite(
        ArrayType(
            element: target.parameter.type
        )
    )

    target.arguments.rewrite(target.arguments)
}
