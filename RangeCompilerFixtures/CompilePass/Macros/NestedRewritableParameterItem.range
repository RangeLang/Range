macro nestedArgumentRewrite(): Parameter { target, diagnostics in
    target.application.expression.replace(with:
        target.application.expression
    )
}

function passthrough(@nestedArgumentRewrite value: Int) -> Int {
    return value
}

#main {
    let output: Int := passthrough(42)
}
