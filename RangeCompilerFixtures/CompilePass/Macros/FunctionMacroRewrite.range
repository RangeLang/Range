macro secondArgument(): Function { target, diagnostics in
    target.call.arguments[1].expression.replace(with:
        target.call.arguments[1].expression
    )
}

@secondArgument
function select(text text: String, number number: Int): String {
    return text
}

@main {
    let result: String(select(text: "Hello", number: 27))
}
