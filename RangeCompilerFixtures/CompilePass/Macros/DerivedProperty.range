macro incremented(by amount: Int): Derived<Int> { target, diagnostics in
    target.getter { value in
        value + amount
    }
}

construct Reader {
    state age: Int   10

    @incremented(by: 1)
    derived next: Int {
        age
    }

    function current() -> Int {
        return next
    }
}
