@main {
    switch FileSystem.readText(path: "../../../.range/Artifacts/03-graph.txt") {
    case .success(let graph):
        Logger.info(graph)
    case .failure:
        Logger.error("failed to read self portrait graph")
    }
}
