enum SwitchReturnValue {
    case first
    case second
}

function describe(_ value: SwitchReturnValue) -> String {
    switch value {
    case .first:
        return "first"
    case .second:
        return "second"
    }
}

#main {
    let value := describe(.first)
}
