@type {
    name: Macro
    fields: {
        @many macros?
        @many parameters?
        name
        target?
        body?: {
            members?
            @many environment?
        }
    }
}
