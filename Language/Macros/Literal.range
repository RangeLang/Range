// Bootstrap primitive: compile a POSIX extended regex and match a whole literal.
// Recognition only; conversion and automatic token dispatch are deferred.
@builtin
macro literal(let pattern: String): Macro
