compilerMacroSnapshot
construct JoinedResult {}

macro joined(): Construct -> JoinedResult { environment in
    let memberCount: Int(environment.target.memberCount("generated"))
    if memberCount == 0 {
        @expand {
            extension #(environment.target.declaration.self) {
                state generated: Int
            }
        }
    }
    return JoinedResult()
}

@joined
construct JoinedTarget {}

function main(): Int {
    return 0
}
