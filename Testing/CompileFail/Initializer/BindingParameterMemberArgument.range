function write(target: binding Int, let: Int) {
    set target   value
}

construct Counter {
    state count: Int(0)

    init() {
        write($self.count, 3)
    }
}

@main {
    let counter: Counter()
}
