name: NeatCLI Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.artifact }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            artifact: neat-macos-arm64
            archive: neat-macos-arm64.lang.tar.gz
            shell: bash
          - os: macos-13
            artifact: neat-macos-x64
            archive: neat-macos-x64.lang.tar.gz
            shell: bash

    defaults:
      run:
        shell: ${{ matrix.shell }}

    steps:
      - uses: actions/checkout@v4

      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.2"

      - name: Build release binary
        working-directory: NeatCLI
        run: swift build -c release --product NeatCLI

      - name: Package macOS install folder
        if: runner.os == 'macOS'
        working-directory: NeatCLI
        run: |
          package="${{ matrix.artifact }}.lang"
          release_version="${GITHUB_REF_NAME#v}"
          if [[ ! "${release_version}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            release_version="$(
              sed -nE 's/.*SemanticVersion\(major: ([0-9]+), minor: ([0-9]+), patch: ([0-9]+)\).*/\1.\2.\3/p' \
                Sources/NeatCLI/NeatVersion.swift \
                | head -n 1
            )"
          fi
          mkdir -p dist
          cp -R ../Packaging/macOS/Neat.lang "dist/${package}"
          mkdir -p "dist/${package}/bin"
          cp .build/release/NeatCLI "dist/${package}/bin/neat"
          printf '%s\n' "$release_version" > "dist/${package}/VERSION"
          chmod 755 "dist/${package}/bin/neat"
          chmod 755 "dist/${package}/install.sh" "dist/${package}/uninstall.sh"
          tar -C dist -czf ${{ matrix.archive }} "$package"

      - name: Package macOS installer package
        if: runner.os == 'macOS'
        working-directory: NeatCLI
        run: |
          release_version="${GITHUB_REF_NAME#v}"
          if [[ ! "${release_version}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            release_version="$(
              sed -nE 's/.*SemanticVersion\(major: ([0-9]+), minor: ([0-9]+), patch: ([0-9]+)\).*/\1.\2.\3/p' \
                Sources/NeatCLI/NeatVersion.swift \
                | head -n 1
            )"
          fi
          package_root="dist/${{ matrix.artifact }}.pkgroot"
          mkdir -p "${package_root}/usr/local/bin"
          cp .build/release/NeatCLI "${package_root}/usr/local/bin/neat"
          chmod 755 "${package_root}/usr/local/bin/neat"
          pkgbuild \
            --root "${package_root}" \
            --identifier "com.georgetchelidze.neat.cli" \
            --version "${release_version}" \
            --install-location "/" \
            "${{ matrix.artifact }}.pkg"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: NeatCLI/${{ matrix.archive }}

      - uses: actions/upload-artifact@v4
        if: runner.os == 'macOS'
        with:
          name: ${{ matrix.artifact }}.pkg
          path: NeatCLI/${{ matrix.artifact }}.pkg

  publish:
    name: Publish GitHub release
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
