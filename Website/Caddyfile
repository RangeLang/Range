{
	admin off
}

{$SITE_ADDRESS:http://localhost} {
	encode zstd gzip

	reverse_proxy website:3000 {
		health_uri /health
		health_interval 30s
		health_timeout 5s
	}

	log {
		output stdout
		format console
	}
}
