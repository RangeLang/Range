# For Loops

## In Statement Blocks

```neat
for item in items {
    print("Item: \\(item)")
}
```

Current loop rules:

- loop syntax is `for name in expression { ... }`
- one loop binding only
- no index binding yet
- no `where` clause yet
