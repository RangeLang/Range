// Exploratory lock macro example.
// This file is for design iteration only.

macro lock: Freestanding<Block> { target, result, diagnostics in
    result.rewrite({
        acquire()
        target()
        release()
    })
}
