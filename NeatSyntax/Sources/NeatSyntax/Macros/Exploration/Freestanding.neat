// Exploratory freestanding macro examples.
// This file is for design iteration only.

macro lock: Freestanding<Block> { block, result, diagnostics in
    result.rewrite({
        acquire()
        block()
        release()
    })
}
