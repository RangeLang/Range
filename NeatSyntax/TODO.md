# NeatSyntax TODO

- Expand synthesized callable and initializer variants from trailing optional parameters into LSP and IDE-facing validation/completion surfaces. The parser and duplicate-signature validation already treat trailing optional parameters as omittable call forms, but editor tooling does not yet surface both invocation shapes.
