enum LoadError {
    case missing
}

@main {
    let output   Channel<String>()

    @background {
        output.send("george")
    }

    state received   output.receive()
    Logger.info(received)
}
