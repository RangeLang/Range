#language
construct Channel<Element> {
    let storage: ChannelStorage<Element>(capacity: 0)

    function send(_ element: Element) {
        storage.send(element: element)
    }

    function receive(): Element {
        return storage.receive()
    }

    function close() {
        storage.close()
    }
}
