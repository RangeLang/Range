interface:
  display_name: "Range Metadata Shapes Publisher"
  short_description: "Keep publisher-facing metadata shapes current."
  default_prompt: "Check this publisher or metadata change against the metadata shapes note."
