# Float Noise Evaluation

Equivalent floating-point noise kernels for C, C++, Rust, Go, Swift, and
TypeScript. Run `noise.ts` directly with Bun for the Bun evaluation, or compile
it with TypeScript 7 (`tsgo`) before running the emitted JavaScript with Bun.
