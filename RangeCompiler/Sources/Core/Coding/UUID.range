@builtin(.storage)
construct UUID {
    state bytes: Int<.unsigned, 8>

    derived count: Int {
        16
    }

    function byte(index: Int): Int {
        return uuidByteAtIndex(uuid: self, index: index)
    }
}

@builtin(.read)
function uuidByteAtIndex(uuid: UUID, index: Int): Int
