@builtin
construct StringStorage {
    derived count: Int
    derived isEmpty: Bool
    function character(index: Int): String
    function substring(start: Int, end: Int): String
    function snakeCase(): String
}
