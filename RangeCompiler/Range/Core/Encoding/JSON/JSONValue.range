enum JSONValue {
    case null
    case bool(value: Bool)
    case float(value: Float)
    case int(value: Int)
    case string(value: String)
    case array(values: Array<JSONValue>)
    case object(fields: Array<JSONField>)
}

construct JSONField {
    @let key: String
    @let value: JSONValue
}
