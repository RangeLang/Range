@construct(name: "Versioned<Value>") {
    @let(name: "current") {
        @value(type: "Value")
    }
    @let(name: "next") {
        @value(type: "Optional<Value>")
    }
    @let(name: "version") {
        @value(type: "Version")
    }
}
