construct AccessLevel {
    let name: String
}

construct AccessLevels {
    let `private`: AccessLevel(name: "private")
    let file: AccessLevel(name: "file")
    let package: AccessLevel(name: "package")
    let `public`: AccessLevel(name: "public")
}
