macro identifierSyntaxName(): Init { target, diagnostics in
    target.application.replace(replacement:
        target.declaration.expression(arguments: [
            String(target.application.argumentsArray<0>.expression)
        ])
    )
}

@syntax
@identifierSyntaxName
construct Identifier {
    let name: String
}
