@syntax
@property
@member
@graph
construct State<T> {
    let identity: GraphIdentity
    let parent: Optional<GraphIdentity>
    let macros: Array<Macro.Application>
    let identifier: Identifier
    let type: TypeReference
    let value: Optional<Expression>

    function initializer(transform: (T) -> T)
    function getter(transform: (T) -> T)
    function setter(transform: (T) -> T)
}
