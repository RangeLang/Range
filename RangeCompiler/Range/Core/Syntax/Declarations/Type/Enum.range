@syntax
construct Enum {
    let declaration: Declaration

    @graph(role: .declaration)
    construct Declaration {
        let self: NominalTypeReference
        let generics: Array<Generic>
        let cases: Array<Case>

        function expand(expansion: () -> Array<@syntax>)
    }

    @graph(role: .declaration)
    construct Case {
        let identifier: Identifier
        let associatedValues: Array<AssociatedValue>
    }

    construct AssociatedValue {
        let label: Optional<String>
        let type: TypeReference
    }
}
