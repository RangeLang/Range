@statement
macro elseif(condition: String = "true") -> String { target, diagnostics in
    @return(value: "statement|kind=elseif|condition="
        + condition
        + "|projection=target.declaration.statements")
}
