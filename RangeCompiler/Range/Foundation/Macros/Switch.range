function switchChildStatementRecords(target: Block): String {
    if target.declaration.bodyText.isEmpty {
        @return(value: "")
    }

    @return(value: "\n" + target.declaration.bodyText)
}

@statement
macro switch(value: String = "") -> String { target, diagnostics in
    @return(value: "statement|kind=switch|value="
        + value
        + "|projection=target.declaration.statements"
        + switchChildStatementRecords(target: target))
}
