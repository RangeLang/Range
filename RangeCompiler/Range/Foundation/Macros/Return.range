@statement
@macro(name: "return", result: "String"){
    @parameter(name: "value") {
        @value(type: "String", current: "String()")
    }
    @state(name: "llvm", value: "\"ret \" + value")

    @if("value.isEmpty") {
        @assignment(target: "llvm", value: "ret void")
    }

    @if("value.contains(value: \"Int(\")") {
        @state(name: "suffix", value: "value.segment(index: 1, delimiter: \"Int(\")")
        @assignment(target: "llvm", value: "\"ret i64 \" + suffix.segment(index: Int(0), delimiter: \")\")")
    }

    @if("value.contains(value: \"Bool(true)\")") {
        @assignment(target: "llvm", value: "ret i1 1")
    }

    @if("value.contains(value: \"Bool(false)\")") {
        @assignment(target: "llvm", value: "ret i1 0")
    }

    @return(value: "statement|kind=return|value="
        + value
        + "|projection="
        + "target.declaration"
        + "|llvm="
        + llvm)
}
