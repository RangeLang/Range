enum Delimiter {
    case comma
}

@macro(name: "variadic", target: "@parameter"){
    @generic(name: "delimiter") {
        @value(type: "Delimiter", current: "Delimiter.comma")
    }
    target.declaration.type.replace(replacement:
        ArrayTypeReference(
            element: target.declaration.type
        )
    )
}
