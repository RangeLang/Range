enum Delimiter {
    case comma
}

macro variadic<delimiter: Delimiter.comma>(): Parameter { target, diagnostics in
    target.declaration.type.replace(replacement:
        ArrayTypeReference(
            element: target.declaration.type
        )
    )
}
