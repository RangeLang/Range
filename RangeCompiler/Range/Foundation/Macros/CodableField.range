construct CodingBehavior {
    @let key: Optional<String>
    @let exclude: Bool
}

macro codable<T>(key: Optional<String> = nil, exclude: Bool = false): @property -> CodingBehavior { target, diagnostics in
    @return(value: CodingBehavior(key: key, exclude: exclude))
}
