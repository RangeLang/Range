@macro(name: "tuple"): Construct { target, diagnostics in
    @let(name: "bindings", value: target.declaration.bindings)

    @if(bindings.count != 2) {
        diagnostics.error("@tuple expects exactly two binding fields.")
    }
}
