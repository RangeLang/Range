macro tuple(): Construct { target, diagnostics in
    let bindings: Array<Binding>(target.declaration.bindings)

    if bindings.count != 2 {
        diagnostics.error("@tuple expects exactly two binding fields.")
    }
}
