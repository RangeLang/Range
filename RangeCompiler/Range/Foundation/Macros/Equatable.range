macro compareProperty(lhs: T, rhs: T, value: Identifier) -> If {
    @generic(name: "T")

    if lhs.#(value) != rhs.#(value) {
        @return(value: false)
    }
}

macro equatableBody(lhs: T, rhs: T, properties: Array<Let>) -> Array<@statement> {
    @generic(name: "T")

    #(properties.map { property in
        @compareProperty(lhs: lhs, rhs: rhs, value: property.identifier)
    })

    @return(value: true)
}

macro equatable(): Construct { target, diagnostics in
    target.declaration.expand {
        extension #(target.declaration.self) {
            function ==(lhs: Self, rhs: Self): Bool {
                @equatableBody(
                    lhs: lhs,
                    rhs: rhs,
                    properties: target.declaration.lets
                )
            }
        }
    }
}
