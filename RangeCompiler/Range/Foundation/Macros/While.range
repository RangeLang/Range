@statement
@macro(name: "while", result: "String"){
    @parameter(name: "condition") {
        @value(type: "String", current: "true")
    }
    @state(name: "bodyText", value: "@statement.bodyText")
    @state(name: "children", value: "String(\"\")")

    @if(!bodyText.isEmpty) {
        @set(name: "children", value: "\n" + bodyText)
    }

    @return(value: "statement|kind=while|condition="
        + condition
        + "|projection="
        + @statement.projection
        + children)
}
