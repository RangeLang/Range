@syntax
construct SplicedSyntax {
    @let delimiter: String
    @let syntax: WrittenSyntax
    @let description: String
}

@construct
@macro(name: "syntax", result: "String")(body: String){ target, diagnostics, graph in
    @return(value: body)
}

@construct
@macro(name: "syntaxCapture")(@literal @spliced(delimiter: "") syntax: @syntax){ target, diagnostics, graph in
    @let(
        name: "splicedParameters",
        value: target.declaration.parameters.filter { parameter in
            @let(
                name: "splicedMacros",
                value: parameter.macros.filter { application in
                    application.identifier.name == "spliced"
                }
            )

            @return(value: splicedMacros.count > 0)
        }
    )

    target.declaration.expand {
        macro #(target.declaration.identifier)(
            #(splicedParameters)
        ): #(target.declaration.self) { target, diagnostics, graph in
        }

        extension #(target.declaration.self) {
            let description: String
        }
    }
}

@parameter
@macro(name: "spliced", result: "@syntax")(delimiter: String = ""){ target, diagnostics in
    @return(value: SplicedSyntax(
        delimiter: delimiter,
        syntax: target.application.syntax,
        description: target.application.syntax.description
    ))
}

@syntax
@macro(name: "splice")(@spliced syntax: @syntax){ target, diagnostics in
    @return(value: syntax)
}

@syntax
@macro(name: "map")(collection: Array<T>, @spliced syntax: @syntax){
    @generic(name: "T")
    @return(value: syntax)
}
