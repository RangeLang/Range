@macro(name: "continue", result: "String", target: "@statement"){
    @return(value: "statement|kind=continue|projection="
        + "target.declaration"
        + "|llvm=br label %loop.condition")
}
