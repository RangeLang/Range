macro self(): Construct -> String { target, diagnostics in
    return target.declaration.identifier.name
}

macro self.lets(): Construct -> String { target, diagnostics in
    return "\(target.declaration.lets)"
}

macro self.states(): Construct -> String { target, diagnostics in
    return "\(target.declaration.states)"
}
