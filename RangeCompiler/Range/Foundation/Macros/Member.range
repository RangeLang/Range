macro member(): Construct | @macro -> Void { target, diagnostics in
}

macro member.declaration.bodyText -> String { target, diagnostics in
    @return(value: target.declaration.bodyText)
}

macro member.ordinal -> String { target, diagnostics in
    @return(value: "\(target.ordinal)")
}
