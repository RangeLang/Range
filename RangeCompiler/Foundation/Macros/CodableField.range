construct CodingBehavior {
    let key: String?
    let exclude: Bool
}

macro codable<T>(_ key: String? = nil, exclude: Bool = false): @property -> CodingBehavior { target, diagnostics in
    return CodingBehavior(key: key, exclude: exclude)
}
