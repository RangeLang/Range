closed macro syntax(): Construct { target, diagnostics in
    target.declaration.expand {
        closed macro #(target.declaration.identifier)(): #(target.declaration.self) { target, diagnostics, graph in
        }
    }
}
