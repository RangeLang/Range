macro literal<T, let delimiter: String? = nil>(
    _ pattern: String = ""
): Function | Construct { target, diagnostics in
    if target.call.arguments.count == 1 && target.call.arguments[0].type == T.self {
        target.call.replace(with:
            target.call.arguments[0].expression
        )
    }
}
