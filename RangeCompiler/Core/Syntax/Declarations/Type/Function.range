@syntax
construct Function {
    let declaration: Declaration
    let call: Call

    @graph(role: .declaration)
    construct Declaration {
        let identity: GraphIdentity
        let parent: GraphIdentity?
        let identifier: Identifier
        let generics: [Generic]
        let parameters: [Parameter.Declaration]
        let returnType: TypeReference?
        let body: Block
    }

    @graph(role: .application)
    construct Call {
        let identifier: Identifier
        let arguments: [Parameter.Application]

        function replace(replacement: Expression): Expression
    }
}
