macro incremented(amount: Int): Derived<Int> { target, diagnostics in
    target.getter { value in
        value + amount
    }
}

construct Reader {
    @state age: Int(10)

    @incremented(amount: 1)
    derived next: Int {
        age
    }

    function current(): Int {
        return next
    }
}
