macro captureText(@capture<Expression> value: Expression): Expression -> String { target, diagnostics in
    target.replace(replacement: "captured: \(value)")
}

@main {
    let text: @captureText(value: 1 + 2)
}
