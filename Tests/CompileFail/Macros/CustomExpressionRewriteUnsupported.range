macro fallbackOne(value: Optional<Int>): Expression { target, diagnostics in
    target.replace(replacement: value ?? 1)
}

@main {
    let maybe: Optional<Int>(nil)
    let resolved: Int(@fallbackOne(value: maybe))
}
