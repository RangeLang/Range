macro incremented(amount: Int): State<Int> { target, diagnostics in
    target.getter { value in
        value + amount
    }
}

construct Reader {
    @incremented(amount: 1)
    @state name: String("George")
}
