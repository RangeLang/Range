enum ValuesToken {
    case values(values: [Int])
    case eof
}

@main {
    let token: ValuesToken(.values(values: [4, 5, 6]))
    switch token {
    case .values(state values):
        values[1]: 9
        return values[1]
    case .eof:
        return 0
    }
}
