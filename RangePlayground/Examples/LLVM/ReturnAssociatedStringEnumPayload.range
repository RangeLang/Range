enum TokenKind {
    case identifier(value: String)
    case eof
}

function token(): TokenKind {
    return TokenKind.identifier(value: String("hello"))
}

@main {
    switch token() {
    case .identifier(let value):
        return value.count
    case .eof:
        return 0
    }
}
