construct Box {
    let value: Int
}

enum BoxToken {
    case value(value: Box)
    case eof
}

@main {
    let token: BoxToken(.value(value: Box(value: 8)))
    switch token {
    case .value(state box):
        box.value: 13
        return box.value
    case .eof:
        return 0
    }
}
