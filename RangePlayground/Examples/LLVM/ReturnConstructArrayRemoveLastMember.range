construct Bag {
    let items: [Int]
}

@main {
    let bag: Bag(items: [1, 2, 3])
    let removed: Optional<Int>(bag.items.removeLast())
    return (removed ?? 0) == 3
        && bag.items.count == 2
        && bag.items.element(index: 1) == 2 ? 0 : 1
}
