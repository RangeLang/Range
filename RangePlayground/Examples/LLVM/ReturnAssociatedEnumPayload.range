enum NumberToken {
    case value(value: Int)
    case eof
}

@main {
    let token: NumberToken(.value(value: 7))
    switch token {
    case .value(let number):
        return number
    case .eof:
        return 0
    }
}
