@main {
    state counts   ["a": 1]
    counts.updateValue(value: 2, key: "b")

    let hasA   counts.contains(key: "a")
    let count   counts.count
    let empty   counts.isEmpty
    let valueA   counts.value(key: "a")
    let removed   counts.removeValue(key: "b")
}
