function write(target _: binding Int, let _: Int) {
    target = value
}

construct Counter {
    state count: Int = 0

    init() {
        write($self.count, 3)
    }
}

@main {
    let counter = Counter()
}
