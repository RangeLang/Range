function write(_ target: binding Int, _ let: Int) {
    target = value
}

construct Counter {
    state count: Int = 0

    init() {
        write($self.count, 3)
    }
}

@main {
    let counter = Counter()
}
