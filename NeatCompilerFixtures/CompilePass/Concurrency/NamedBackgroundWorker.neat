@background workerA(count: Int) -> Promise<String, String> {
    if count == 0 {
        return "zero"
    }

    return "done"
}

@main {
    value promise: Promise<String, String> = workerA(count: 2)
    Logger.info("started named worker")
}
