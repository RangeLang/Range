@main {
    let queue = Channel<Int>.withCapacity(2)

    @background {
        queue.send(1)
        queue.send(2)
        queue.send(3)
        Logger.info("producer finished")
    }

    Logger.info(queue.receive())
    Logger.info(queue.receive())
    Logger.info(queue.receive())
}
