enum LoadError {
    case missing
}

@main {
    let output = Channel<String>()

    @background {
        output.send("george")
    }

    let received: String = output.receive()
    Logger.info(received)
}
