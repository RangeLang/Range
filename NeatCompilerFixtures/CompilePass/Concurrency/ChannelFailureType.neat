enum LoadError {
    case missing
}

@main {
    value output = Channel<String>()

    @background {
        output.send("george")
    }

    value received: String = output.receive()
    Logger.info(received)
}
