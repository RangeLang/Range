function refreshCache() {
    Logger.info("refresh requested")

    @background {
        Logger.info("background refresh started")
        let finished = true
        Logger.info("background refresh finished")
    }

    Logger.info("refresh function returned immediately")
}

@main {
    refreshCache()
}
