enum LoadError {
    case missing
}

@main {
    value output = Channel<String>()

    @background {
        output.send("george")
    }

    state received = output.receive()
    Logger.info(received)
}
