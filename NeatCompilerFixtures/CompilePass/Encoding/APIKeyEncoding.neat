function encodeAPIKeyData() -> String {
    let encoder = APIKeyEncoding()
    let value = Data(storage: DataStorage())
    return encoder.encode(value)
}

function decodeAPIKeyText() -> Result<Data, DecodingError> {
    let encoder = APIKeyEncoding()
    return encoder.decode("")
}

#main {
}
