macro fallbackOne(value _: Optional<Int>): Expression { target, diagnostics in
    target.rewrite(value ?? 1)
}

@main {
    let maybe: Int? = nil
    let resolved: Int = #fallbackOne(value: maybe)
}
