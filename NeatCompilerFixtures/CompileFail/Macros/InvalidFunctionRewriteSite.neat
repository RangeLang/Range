macro badFunctionRewrite(): Function { target, diagnostics in
    target.rewrite(target.application.arguments[0].expression)
}

#badFunctionRewrite
function example(value _: Int) -> Int {
    return value
}

@main {
    value result: Int = example(1)
}
