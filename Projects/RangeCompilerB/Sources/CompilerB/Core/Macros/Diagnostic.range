macro diagnostic(): @member -> Void {
  if #environment.target.value.RHS != String {
    #environment.diagnostic(
      "Diagnostic RHS needs to be of type String"
    )
  }

  if !#environment.target.value.isTarget {
    #environment.diagnostic(
      #environment.target.value.RHS
    )
  }
}
