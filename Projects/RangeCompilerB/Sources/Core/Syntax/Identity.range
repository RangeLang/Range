macro identitySyntaxName(): Init {
    #environment.target.application.replace(with:
        #environment.target.Declaration.expression(arguments: [
            String(#environment.target.application.arguments[0].expression)
        ])
    )
}

@syntax
@identitySyntaxName
@hashable
construct Identity {
    let name: String
    let id: UUID
    let parent: Identity?
    let source: @syntax?
}
