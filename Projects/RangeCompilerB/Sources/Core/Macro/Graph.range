enum GraphRole {
    case declaration
    case application
}

macro graph(
    _ role: GraphRole,
    _ additionalRole: GraphRole?
): Construct -> Void {
    #environment.graph.addNode(role: role, additionalRole: additionalRole)
}
