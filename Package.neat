construct Neat: Package {
    let version: String = "0.1.8"
    let author: String = "George"
    let remotes: [Remote] = [
        Remote(url: "https://github.com/georgetchelidze/Neat.git"),
        Remote(url: "git@github.com:georgetchelidze/Neat.git"),
    ]
}
